// A scratch file for poking at the editor integration.
// Nothing here parses yet — the tokenizer and parser are still empty.

struct Point {
  x: s64,
  y: s64,
}

fn add(a: u64, b: u64) -> u64 {
  a + b
}

let origin = Point { x: 0, y: 0 };
let mut total = 0;

while total < 10 {
  total = add(total, 1);
}

total
